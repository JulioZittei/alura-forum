server:
  servlet:
    context-path: /api/v1


spring:
  application:
    name: alura-forum
  profiles:
    active: @activatedProperties@

management:
  endpoints:
    web:
      exposure:
        include: health, prometheus